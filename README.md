# Phạm Minh Ánh — Marketing Portfolio

Website tĩnh được đóng gói sẵn cho Docker/OrbStack.

## Chạy bằng OrbStack

```bash
docker compose up --build -d
```

Mở `http://localhost:8088`.

## Dừng website

```bash
docker compose down
```
